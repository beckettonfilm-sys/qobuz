Sprawdź wszystkie pliki w repozytorium. Rozwiąż problem z tworzeniem folderów i kontenerów. Cały proces trwa wolno, po stworzeniu jakiegoś folderu, użytkownik musi zaczekać pewien czas zanim aplikacja pozwoli mu stwrzyć kolejny folder lub kontener. W szczególności sprawdź pliki TXT:  debug-log i korekta. W debug-log jest dokładny zapis czasów jaki mija od tworzenia przez użytkownika kolejnych folderów: TEST_1, TEST_2, TEST_3, TEST_4, TEST_5. Z koleji w korekta.txt jest tekt chataGPT, który podsumuwuje wolne działanie aplikacji, jeśli chodzi o proces twrzenia i usuwania folderów. Wyciągnij z niego wnioski i zstosuj do kodu, jeżeli trzeba jakiś plik przerobic od zera to to zrób. Druga rzecz, która wymaga poprawy to EDYCJA i USUWANIE folderów i kontenerów w programie. Obecnie nie działa ta funkcja. Sprawdź co jest źródłem problemu i go rozwiąż. Nie działa też wyszukiwarka w programie, ten problem też możesz rozwiązać.